name: 🚀 Sentinel Quantum Autonomous Cycle v11.4

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"  # ⏰ کارکردن بەخۆکار هەموو 6 کاتژمێر جارێک

jobs:
  quantum-cycle:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      # 1️⃣ چێک‌ئاووتی پڕۆژە
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ دامەزراندنی پایتۆن
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ دامەزراندنی پەیوەندیدارەکان
      - name: 📚 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests aiohttp beautifulsoup4 lxml python-nmap shodan dnspython httpx trio numpy pandas scikit-learn torch transformers sentencepiece tldextract

      # 4️⃣ پێدانی لیستی تارگێت
      - name: 🧠 Generate 500 Authorized Targets
        run: |
          echo "🔄 Generating global_500_targets.txt..."
          mkdir -p data/targets
          cd src
          python generate_targets.py
          cd ..
          echo "✅ Target list ready at data/targets/global_500_targets.txt"

      # 5️⃣ دەستپێکردنی کۆنتڕۆڵەری سەرەکی
      - name: 🚀 Launch Main Quantum Controller
        run: |
          echo "=============================================="
          echo "⚡ Running Digital Sentinel Quantum v11.4"
          echo "=============================================="
          cd src

          ATTEMPT=1
          MAX_ATTEMPTS=3
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              echo "▶️ Attempt $ATTEMPT of $MAX_ATTEMPTS..."
              python main_controller_v11_4_quantum.py && break
              EXIT_CODE=$?
              echo "⚠️ Attempt $ATTEMPT failed with code $EXIT_CODE"
              ATTEMPT=$((ATTEMPT+1))
              if [ $ATTEMPT -le $MAX_ATTEMPTS ]; then
                  echo "♻️ Retrying in 30 seconds..."
                  sleep 30
              else
                  echo "❌ All attempts failed."
                  exit 1
              fi
          done

      # 6️⃣ وۆچ‌دۆگ بۆ بەردەوامی کارکردن
      - name: 🐺 Watchdog Auto-Loop
        if: always()
        run: |
          echo "🐺 Watchdog is active – Sentinel will relaunch in 10 minutes"
          sleep 600
          gh workflow run "Sentinel Quantum Autonomous Cycle v11.4"

      # 7️⃣ بارکردنی لاگ
      - name: 📤 Upload Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-logs
          path: data/logs/

      # 8️⃣ بارکردنی ڕاپۆرتەکان
      - name: 📑 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-reports
          path: data/reports/

      # 9️⃣ تەواوکردنی سایکل
      - name: ✅ Finalize Cycle
        run: echo "✅ Quantum Autonomous Cycle completed successfully."
