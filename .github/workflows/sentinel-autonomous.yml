name: 🚀 Sentinel Quantum Autonomous Cycle

on:
  workflow_dispatch:

jobs:
  run-quantum-scan:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📚 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: ⚙️ Generate Authorized Targets
        run: |
          echo "🧠 Generating 500 authorized bug bounty targets..."
          cd src
          python generate_targets.py
          cd ..
          echo "✅ Targets saved in data/targets.txt"
          cat data/targets.txt | head -n 10

      - name: 🚀 Launch Main Controller with Recovery
        run: |
          echo "========================================"
          echo "🚀 LAUNCHING Sentinel Quantum Main Controller v11.3"
          echo "========================================"
          cd src
          python main_controller_v11_3_autonomous.py || exit 1

      - name: 📤 Upload Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-logs
          path: data/logs/

      - name: 📑 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-reports
          path: data/reports/

      - name: ✅ Finalize Job
        run: echo "✅ Sentinel Quantum Autonomous Cycle complete."
