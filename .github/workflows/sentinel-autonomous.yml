name: Digital Sentinel v11 – Eternal Autonomous Mode

on:
  workflow_dispatch:

jobs:
  recon-cycle:
    runs-on: ubuntu-latest
    timeout-minutes: 180   # ⏱️ 3 hours max runtime

    steps:
    - name: 📦 Checkout Repository
      uses: actions/checkout@v4

    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 🧰 Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt || true

    - name: 🚀 Launch Main Controller
      working-directory: ./src     # ⬅️ Execute inside src folder
      run: |
        echo ">>> Launching main controller..."
        python3 main_controller_v11.py

    - name: 🧠 Launch Sentinel Eternal Hunter
      working-directory: ./src     # ⬅️ Also execute inside src
      run: |
        echo ">>> Starting sentinel_eternal_hunter.py"
        python3 sentinel_eternal_hunter.py

    - name: 🪶 Upload Logs & Reports
      uses: actions/upload-artifact@v4
      with:
        name: digital-sentinel-v11-reports
        path: |
          data/logs/
          data/reports/

    - name: 🛰️ Send Discord Summary
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      run: |
        echo ">>> Sending mission summary to Discord..."
        python3 src/discord_notify.py "Cycle Complete" "Sentinel v11 autonomous mode finished successfully ✅"

    - name: ✅ Complete Sentinel Cycle
      run: echo "🎯 Sentinel cycle completed successfully at $(date)"
