name: "Digital Sentinel v6.0 – Autonomous Mode"

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"   # هر ٣٠ خولەک کار بکات بەردەوام

jobs:
  recon-cycle:
    runs-on: ubuntu-latest
    steps:
      - name: 🔹 Checkout
        uses: actions/checkout@v4

      - name: 🧰 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 🧠 Install Dependencies
        run: |
          pip install -r requirements.txt || true

      - name: 🚀 Start Autonomous Loop
        run: |
          python3 src/main_controller.py
