name: "🚀 Sentinel Autonomous Cycle"

on:
  workflow_dispatch:

jobs:
  run-scan:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 📁 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ▶️ Launch Main Controller
        run: |
          python src/main_controller_v11_1.py

      # OPTIONAL — Save logs/reports if needed
      - name: 📤 Upload Logs
        uses: actions/upload-artifact@v4
        with:
          name: logs
          path: data/logs/

      - name: 📤 Upload Reports
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: data/reports/
